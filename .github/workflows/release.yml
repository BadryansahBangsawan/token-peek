name: Release
on:
  push:
    tags: ["v*"]
permissions:
  contents: write
jobs:
  release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Package
        run: bash package-app.sh
      - name: Zip and upload
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          APP=$(ls -d dist/*.app)
          BASE=$(basename "$APP")
          ditto -c -k --keepParent "$APP" "$BASE.zip"
          gh release upload "$GITHUB_REF_NAME" "$BASE.zip" --clobber || gh release create "$GITHUB_REF_NAME" "$BASE.zip" --title "${GITHUB_REF_NAME#v}" --generate-notes
